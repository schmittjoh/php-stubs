<?php

/**
 * Match ETag
 *
 * @phpstub
 *
 * @param string $etag
 * @param bool $for_range
 *
 * @return bool Returns true if  matches or the header contained the asterisk ("*"), else false.
 */
function http_match_etag($etag, $for_range = false)
{
}