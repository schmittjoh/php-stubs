<?php

/**
 * Decode MIME header elements
 *
 * @phpstub
 *
 * @param string $text
 *
 * @return array The decoded elements are returned in an array of objects, where each
 *               object has two properties,  and
 *               .
 */
function imap_mime_header_decode($text)
{
}