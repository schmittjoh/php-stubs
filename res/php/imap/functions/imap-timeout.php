<?php

/**
 * Set or fetch imap timeout
 *
 * @phpstub
 *
 * @param int $timeout_type
 * @param int $timeout
 *
 * @return mixed If the  parameter is set, this function
 *               returns true on success and false on failure.
 */
function imap_timeout($timeout_type, $timeout = -1)
{
}