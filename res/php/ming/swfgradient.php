<?php

/** @phpstub */
class SWFGradient
{
    /**
     * Creates a gradient object
     */
    public function __construct()
    {
    }

    /**
     * Adds an entry to the gradient list
     *
     * @param float $ratio
     * @param int $red
     * @param int $green
     * @param int $blue
     * @param int $alpha
     *
     * @return void 
     */
    public function addEntry($ratio, $red, $green, $blue, $alpha = 255)
    {
    }
}