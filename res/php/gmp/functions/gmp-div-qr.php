<?php

/**
 * Divide numbers and get quotient and remainder
 *
 * @phpstub
 *
 * @param resource $n
 * @param resource $d
 * @param int $round
 *
 * @return array Returns an ``array``, with the first
 *               element being  (the integer result of the
 *               division) and the second being
 *               (the remainder of the division).
 */
function gmp_div_qr($n, $d, $round = false)
{
}