<?php

/**
 * Returns the next row from a query as an associative or numeric array
 *
 * @phpstub
 *
 * @param resource $statement
 * @param int $mode
 *
 * @return array Returns an array with associative and/or numeric indices. If there
 *               are no more rows in the  then
 *               false is returned.
 */
function oci_fetch_array($statement, $mode = NULL)
{
}