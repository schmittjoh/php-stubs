<?php

/**
 * Get the path to the current category
 *
 * @phpstub
 *
 * @param resource $agent
 * @param string $category
 *
 * @return array The returned array consists of pairs. Elements with even index numbers
 *               contain the category paths, odd elements contain the corresponding category
 *               names.
 */
function udm_cat_path($agent, $category)
{
}