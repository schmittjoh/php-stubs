<?php

/**
 * Returns the database schema
 *
 * @phpstub
 *
 * @param resource $pxdoc
 * @param int $mode
 *
 * @return array Returns the schema of a database file as an associated array. The key name
 *               is equal to the field name. Each array element is itself an associated
 *               array containing the two fields  and
 *               .  is one of the constants in
 *               table .  is the number of bytes this field
 *               consumes in the record. The total of all field sizes is equal to the
 *               record size as it can be retrieved with .
 */
function px_get_schema($pxdoc, $mode = false)
{
}