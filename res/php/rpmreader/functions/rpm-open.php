<?php

/**
 * Opens an RPM file
 *
 * @phpstub
 *
 * @param string $filename
 *
 * @return resource If the open succeeds, then  will
 *                  return a file pointer resource to the newly opened file.  On
 *                  error, the function will return false.
 */
function rpm_open($filename)
{
}