<?php

/**
 * Get options from an XML parser
 *
 * @phpstub
 *
 * @param resource $parser
 * @param int $option
 *
 * @return mixed This function returns false if  does
 *               not refer to a valid parser or if  isn't
 *               valid (generates also a ).
 *               Else the option's value is returned.
 */
function xml_parser_get_option($parser, $option)
{
}