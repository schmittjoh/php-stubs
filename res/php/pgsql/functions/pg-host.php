<?php

/**
 * Returns the host name associated with the connection
 *
 * @phpstub
 *
 * @param resource $connection
 *
 * @return string A ``string`` containing the name of the host the
 *                is to, or false on error.
 */
function pg_host($connection = NULL)
{
}