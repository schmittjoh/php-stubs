<?php

/**
 * Computes the intersection of arrays with additional index check, compares indexes by a callback function
 *
 * @phpstub
 *
 * @param array $array1
 * @param array $array2
 * @param array $_
 * @param callable $key_compare_func
 *
 * @return array Returns the values of  whose values exist
 *               in all of the arguments.
 */
function array_intersect_uassoc($array1, $array2, $_ = array(), $key_compare_func)
{
}