<?php

/**
 * Computes the intersection of arrays with additional index check, compares data and indexes by a callback functions
 *
 * @phpstub
 *
 * @param array $array1
 * @param array $array2
 * @param array $_
 * @param callable $data_compare_func
 * @param callable $key_compare_func
 *
 * @return array Returns an array containing all the values of
 *               that are present in all the arguments.
 */
function array_uintersect_uassoc($array1, $array2, $_ = array(), $data_compare_func, $key_compare_func)
{
}