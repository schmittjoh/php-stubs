<?php

/**
 * Computes the intersection of arrays using keys for comparison
 *
 * @phpstub
 *
 * @param array $array1
 * @param array $array2
 * @param array $_
 *
 * @return array Returns an associative array containing all the entries of
 *               which have keys that are present in all
 *               arguments.
 */
function array_intersect_key($array1, $array2, $_ = array())
{
}