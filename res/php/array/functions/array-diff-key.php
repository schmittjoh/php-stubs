<?php

/**
 * Computes the difference of arrays using keys for comparison
 *
 * @phpstub-variable-parameters
 * @phpstub
 *
 * @param array $array1
 * @param array $array2
 * @param array $_
 *
 * @return array Returns an ``array`` containing all the entries from
 *               whose keys are not present in any of the
 *               other arrays.
 */
function array_diff_key($array1, $array2, $_ = array())
{
}