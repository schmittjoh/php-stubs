<?php

/**
 * Set options on a given XSLT processor
 *
 * @phpstub
 *
 * @param resource $processor
 * @param int $newmask
 *
 * @return mixed Returns the number of previous mask is possible, true otherwise,
 *               false in case of an error.
 */
function xslt_setopt($processor, $newmask)
{
}