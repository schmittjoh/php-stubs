<?php

/**
 * Maps global id on virtual local id
 *
 * @phpstub
 *
 * @param int $connection
 * @param int $server_id
 * @param int $object_id
 *
 * @return int Returns the virtual object id.
 */
function hw_mapid($connection, $server_id, $object_id)
{
}