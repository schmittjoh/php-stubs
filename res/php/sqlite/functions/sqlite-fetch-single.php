<?php

/**
 * Fetches the first column of a result set as a string
 *
 * @phpstub
 *
 * @param resource $result
 * @param bool $decode_binary
 *
 * @return string
 */
function sqlite_fetch_single($result, $decode_binary = true)
{
}

/**
 * Fetches the first column of a result set as a string
 *
 * @phpstub
 *
 * @param bool $decode_binary
 *
 * @return string
 */
function sqlite_fetch_single($decode_binary = true)
{
}

/**
 * Fetches the first column of a result set as a string
 *
 * @phpstub
 *
 * @param bool $decode_binary
 *
 * @return string
 */
function sqlite_fetch_single($decode_binary = true)
{
}

/**
 * Fetches the first column of a result set as a string
 *
 * @phpstub
 *
 * @param resource $result
 * @param bool $decode_binary
 *
 * @return string
 */
function sqlite_fetch_string($result, $decode_binary = true)
{
}

/**
 * Fetches the first column of a result set as a string
 *
 * @phpstub
 *
 * @param bool $decode_binary
 *
 * @return string
 */
function sqlite_fetch_string($decode_binary = true)
{
}

/**
 * Fetches the first column of a result set as a string
 *
 * @phpstub
 *
 * @param bool $decode_binary
 *
 * @return string
 */
function sqlite_fetch_string($decode_binary = true)
{
}