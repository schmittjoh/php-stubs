<?php

/**
 * Define a terminal color
 *
 * @phpstub
 *
 * @param int $color
 * @param int $r
 * @param int $g
 * @param int $b
 *
 * @return int Returns  if the function was successful,
 *             and  if ncurses or terminal
 *             color capabilities have not been initialized or the terminal does not
 *             have color changing capabilities.
 */
function ncurses_init_color($color, $r, $g, $b)
{
}