<?php

/**
 * Returns an error message
 *
 * @phpstub
 *
 * @param resource $radius_handle
 *
 * @return string Returns error messages as string from failed radius functions.
 */
function radius_strerror($radius_handle)
{
}