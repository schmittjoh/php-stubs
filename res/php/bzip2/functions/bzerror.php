<?php

/**
 * Returns the bzip2 error number and error string in an array
 *
 * @phpstub
 *
 * @param resource $bz
 *
 * @return array Returns an associative array, with the error code in the
 *               entry, and the error message in the
 *               entry.
 */
function bzerror($bz)
{
}