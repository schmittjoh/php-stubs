<?php

/**
 * Prepare a query for later binding of parameter placeholders and execution
 *
 * @phpstub
 *
 * @param string $query
 *
 * @return resource
 */
function ibase_prepare($query)
{
}

/**
 * Prepare a query for later binding of parameter placeholders and execution
 *
 * @phpstub
 *
 * @param resource $link_identifier
 * @param string $query
 *
 * @return resource
 */
function ibase_prepare($link_identifier, $query)
{
}

/**
 * Prepare a query for later binding of parameter placeholders and execution
 *
 * @phpstub
 *
 * @param resource $link_identifier
 * @param string $trans
 * @param string $query
 *
 * @return resource
 */
function ibase_prepare($link_identifier, $trans, $query)
{
}