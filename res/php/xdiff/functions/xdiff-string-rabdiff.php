<?php

/**
 * Make binary diff of two strings using the Rabin's polynomial fingerprinting algorithm
 *
 * @phpstub
 *
 * @param string $old_data
 * @param string $new_data
 *
 * @return string Returns string with binary diff containing differences between "old" and "new"
 *                data or false if an internal error occurred.
 */
function xdiff_string_rabdiff($old_data, $new_data)
{
}