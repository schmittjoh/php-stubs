<?php

/**
 * Change file/direcrory permissions.
 *
 * @phpstub
 *
 * @param string $path
 * @param int $mode
 * @param int $pri
 * @param callable $callback
 * @param mixed $data
 *
 * @return resource returns request resource on success or false on error.
 */
function eio_chmod($path, $mode, $pri = 0, $callback = 'NULL', $data = 'NULL')
{
}