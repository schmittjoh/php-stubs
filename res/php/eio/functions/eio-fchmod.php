<?php

/**
 * Change file permissions.
 *
 * @phpstub
 *
 * @param mixed $fd
 * @param int $mode
 * @param int $pri
 * @param callable $callback
 * @param mixed $data
 *
 * @return resource returns request resource on success or false on error.
 */
function eio_fchmod($fd, $mode, $pri = 0, $callback = 'NULL', $data = 'NULL')
{
}