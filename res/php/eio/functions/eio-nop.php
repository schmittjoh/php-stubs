<?php

/**
 * Does nothing, except go through the whole request cycle.
 *
 * @phpstub
 *
 * @param int $pri
 * @param callable $callback
 * @param mixed $data
 *
 * @return resource returns request resource on success or false on error.
 */
function eio_nop($pri = 0, $callback = 'NULL', $data = 'NULL')
{
}