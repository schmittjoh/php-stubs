<?php

/**
 * Convert a unicode string in any encoding
 *
 * @phpstub
 *
 * @param unicode $input
 * @param string $encoding
 * @param int $errmode
 *
 * @return string A ``string`` on success.
 */
function unicode_encode($input, $encoding, $errmode = NULL)
{
}