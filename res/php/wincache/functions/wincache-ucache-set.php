<?php

/**
 * Adds a variable in user cache and overwrites a variable if it already exists in the cache
 *
 * @phpstub
 *
 * @param mixed $key
 * @param mixed $value
 * @param int $ttl
 *
 * @return bool
 */
function wincache_ucache_set($key, $value, $ttl = 0)
{
}

/**
 * Adds a variable in user cache and overwrites a variable if it already exists in the cache
 *
 * @phpstub
 *
 * @param array $values
 * @param mixed $unused
 * @param int $ttl
 *
 * @return bool
 */
function wincache_ucache_set($values, $unused = NULL, $ttl = 0)
{
}