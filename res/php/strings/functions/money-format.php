<?php

/**
 * Formats a number as a currency string
 *
 * @phpstub
 *
 * @param string $format
 * @param float $number
 *
 * @return string Returns the formatted string. Characters before and after the formatting
 *                string will be returned unchanged.
 *                Non-numeric  causes returning null and
 *                emitting .
 */
function money_format($format, $number)
{
}