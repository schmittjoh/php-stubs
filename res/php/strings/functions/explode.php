<?php

/**
 * Split a string by string
 *
 * @phpstub
 *
 * @param string $delimiter
 * @param string $string
 * @param int $limit
 *
 * @return array Returns an ``array`` of ``string``s
 *               created by splitting the  parameter on
 *               boundaries formed by the .
 */
function explode($delimiter, $string, $limit = NULL)
{
}