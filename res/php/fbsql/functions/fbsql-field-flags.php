<?php

/**
 * Get the flags associated with the specified field in a result
 *
 * @phpstub
 *
 * @param resource $result
 * @param int $field_offset
 *
 * @return string Returns the field flags of the specified field as a single word per flag
 *                separated by a single space, so that you can split the returned value
 *                using .
 */
function fbsql_field_flags($result, $field_offset = NULL)
{
}