<?php

/**
 * Return numerically indexed array of registered superglobals
 *
 * @phpstub
 *
 * @return array Returns a numerically indexed array of the currently registered superglobals.
 *               i.e. _GET, _POST, _REQUEST, _COOKIE, _SESSION, _SERVER, _ENV, _FILES
 */
function runkit_superglobals()
{
}